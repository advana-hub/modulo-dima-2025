name: Create Release with ZIP

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Create ZIP file
      run: zip -r modulo-dima-2025.zip . || exit 1

    - name: Get current date
      id: date
      run: |
        echo "date=$(date +'%Y.%m.%d')" >> $GITHUB_OUTPUT
        echo "timestamp=$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_OUTPUT

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: v${{ steps.date.outputs.timestamp }}
        release_name: Release ${{ steps.date.outputs.timestamp }}
        draft: false
        prerelease: false

    - name: Upload ZIP file to Release
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./modulo-dima-2025.zip
        asset_name: modulo-dima-2025.zip
        asset_content_type: application/zip
